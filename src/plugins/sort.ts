import type { Plugin, SortedPlugins, Hook } from "./plugin";

export function sortPlugins(plugins: Plugin[]): SortedPlugins {
  return;
}

export function sortPluginsHook(plugins: Plugin[], hook: Hook): SortedPlugins {
  return;
}
